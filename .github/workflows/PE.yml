name: Extraction PE
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
    - name: Exec ls
      run: |
        ls

    - name: Exec python PE
      env:
        API_KEY: ${{ secrets.API_KEY }}
      run: |
        cd /home/runner/work/projet_fil_rouge/projet_fil_rouge/
        python3 PE.py

