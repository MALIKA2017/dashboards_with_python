name: Extraction PE
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
    - name: Exec python PE
      env:
        API_KEY: ${{ secrets.API_KEY }}
      run: |
        echo "GITHUB_WORKSPACE: ${GITHUB_WORKSPACE}"
        cd ${GITHUB_WORKSPACE}
        python3 PE.py

